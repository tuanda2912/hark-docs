# Hark Wiki — Log

## [2026-06-05] bootstrap | initial wiki from 38 ADRs + core docs + code subsystem map
Created the initial Hark project wiki: 19 subsystem pages, 7 concept pages, 9 decision digests (covering all 38 ADRs), 1 glossary, and 1 overview, all catalogued in `index.md` by category (Overview → Subsystems → Concepts → Decisions → Glossary). Sources ingested: `CLAUDE.md`, `STATUS.md`, `meetingmind-handoff.md`, `docs/BACKLOG.md`, `docs/decisions/*.md`, and a subsystem map across `engine/` and `ui/`. Supersession links recorded per the schema (0035→0037, 0019→0036, 0021→0038, 0017 superseding 0016's pipeline choice, 0007 amending 0006).

## [2026-06-05] move | relocated into hark-docs
Moved the wiki out of the code repo (`hark/docs/wiki/`) into `hark-docs/wiki/`, keeping it separate from the source it documents (per the user's call). Re-pointed all 177 ADR citations from relative paths to GitHub links (`github.com/tuanda2912/hark/blob/main/docs/decisions/…`), since the ADRs live in the sibling `tuanda2912/hark` repo. A prior lint pass fixed 180+ broken links; all internal `[[wikilinks]]` + relative links now resolve.

## [2026-06-05] rebuild | grounded subsystem pages in the understand-anything knowledge graph
Ran `/understand-anything:understand` over the code repo (code-only scope; 127 source files → **280 nodes / 445 edges / 7 layers / 12-step tour**; `hark/.understand-anything/knowledge-graph.json`, commit `8efdfde`). Enriched all **19 subsystem pages in place** (19 parallel agents) with a verified `## Code map`: files + one-line summaries, key types/functions with line ranges, the XCTest suites that pin them, and deduped cross-subsystem edges (`imports`/`calls`/`depends_on`/`implements`, each rendered as a wikilink to the related subsystem page). Built deterministic per-subsystem graph slices first (92 files mapped to the 19 subsystems, 15 build-config, 17 tests, 3 tooling skipped — full 127/127 coverage). Reconciled drift: confirmed grow-in-place is governed by **ADR-0036** (not the stale ADR-0020 label) and that live-translation surfaces are gone (**ADR-0037**), with `translateSegment`/`TranslationJobService` surviving only as on-demand post-stop. Added an *Architectural layers* map to `overview.md` and refreshed the `WIKI.md` scope note. Preserved all hand-written prose, ADR citations, and supersession links. Verified 1160 wikilinks resolve. Caveat recorded: the graph does not resolve Swift module imports, so engine cross-file wiring uses `depends_on`/`calls`/`implements` rather than `imports`.

## [2026-06-05] onboard | generated an onboarding guide from the knowledge graph
Ran `/understand-anything:understand-onboard` against `hark/.understand-anything/knowledge-graph.json`. Produced a newcomer's guide (project overview · 7 architecture layers · key concepts · the 12-step guided tour · file map by layer · complexity hotspots — 48 `complex` file-level nodes, the ~8 highest-leverage ones called out). Saved **two copies** (per the user's call): a plain contributor copy at the code repo's `docs/ONBOARDING.md`, and this wiki-integrated copy at `wiki/onboarding.md` (frontmatter + per-subsystem wikilinks + a GitHub cross-link to the contributor copy). Catalogued under *Overview* in `index.md`.

## [2026-06-05] note | documented the Obsidian graph-view recipe
Added a **"Viewing this wiki as a graph (Obsidian)"** section to `WIKI.md`: vault setup, the five role-based color-group queries (🔵 engine / 🟢 ui / 🔴 main+privacy / 🟣 concepts / 🟠 decisions), de-hairball force values (center 0.2 · repel 19 · link 0.4 · distance 200 · thin lines), filter recipes (peel-hubs, `path:subsystems`, egress-only), and the local-graph (depth-2) comprehension tip. The live settings are applied in `.obsidian/graph.json`, which is tracked (only the volatile `.obsidian/workspace*.json` is gitignored) so the colored view travels with the repo; the `WIKI.md` recipe is the recoverable source of truth if that config is ever lost.
